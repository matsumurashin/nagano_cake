<h4>商品一覧</h4>

<div class="container">
  <% @item.each do |item| %>
  <div class="row">
    <div class="col-3">
      <%= image_tag item.get_image(300,300) %>
      <%= link_to item.name,item_path(item.id) %><br>
      ¥<%= item.price %>
    </div>
  </div>
  <% end %>
</div>